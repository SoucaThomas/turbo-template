{
  "name": "@turbo-template/stripe-plans",
  "version": "1.0.0",
  "description": "Shared Stripe plans configuration for turbo-template",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  },
  "peerDependencies": {
    "stripe": "^14.0.0"
  },
  "files": ["dist/**/*"]
}
